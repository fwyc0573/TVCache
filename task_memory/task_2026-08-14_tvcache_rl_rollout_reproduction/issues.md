## Modification History

| Date       | Summary of Changes |
|------------|--------------------|
| 2026-09-10 | Recorded the reached Torch cu117 / Hopper kernel incompatibility and same-version cu118 remediation. |
| 2026-09-09 | Recorded resolved provider accounting gaps and ongoing asset transfer/runtime prerequisites. |
| 2026-09-09 | Closed I-055 disk gate after the user freed space and authorized continuation with H800 as a fallback. |
| 2026-09-08 | Resolved the H200 credential gate and StepCode protocol contract; recorded provider rejection of `json_schema` and the successful `json_object` plus standard tool-message path. |
| 2026-09-08 | Added the H200 worker credential blocker after the specified StepCast image reached `Ready` but reported `STEPCODE_API_KEY=UNSET`. |
| 2026-09-03 | Added the Tinker organization billing blocker observed from the authenticated SDK capability request. |
| 2026-09-03 | Opened a credential-rotation gate after the full Tinker key was supplied in chat; only value-free presence checks are permitted. |
| 2026-09-03 | Reopened the Tinker credential gate after the reported zshrc update was absent from all checked startup files and value-free shell checks remained unset. |
| 2026-08-17 | Resolved I-048 after key rotation and updated I-010 for the approved DeepSeek/local-vision topology. |
| 2026-08-17 | Added the disclosed DeepSeek credential rotation blocker without recording the secret. |
| 2026-08-16 | Refreshed I-010 after the Phase 4 credential gate confirmed both required API keys remain unset. |
| 2026-08-16 | Resolved I-047 after v9 architecture CLEAR and code/spec/security APPROVE. |
| 2026-08-16 | Recorded the CPU-verified production convergence remediation for I-047 pending repeat architecture review. |
| 2026-08-16 | Extended I-047 with the v8 production-entrypoint convergence blocker and reopened CPU remediation. |
| 2026-08-16 | Recorded the implemented and CPU-verified I-047 contract pending v8 review, and closed I-046 from the v7 normal-path confirmation. |
| 2026-08-16 | Added I-047 from the v7 architecture BLOCK and reopened failure-path teardown remediation. |
| 2026-08-16 | Recorded the implemented I-046 lifecycle contract and fresh 160-test gate while retaining the issue for v7 review closure. |
| 2026-08-16 | Added I-046 from the v6 architecture review and reopened task-level cache teardown remediation. |
| 2026-08-16 | Resolved I-044 and I-045 with unified temporary-environment pending ownership. |
| 2026-08-16 | Added I-044 and I-045 from the v5 architecture review and reopened lifecycle remediation. |
| 2026-08-16 | Added I-042 and I-043 from the v4 code/spec/security review and reopened lifecycle remediation. |
| 2026-08-16 | Closed I-035 through I-040 after lifecycle/path/binding rollback tests and deferred unreachable multi-batch I-041. |
| 2026-08-16 | Added the v3 review blockers and lifecycle watch items as I-035 through I-041. |
| 2026-08-16 | Recorded generated bytecode/log cleanup requiring explicit deletion permission. |
| 2026-08-16 | Closed I-022 through I-030 and I-033 after the real preflight and fresh Python 3.12 CPU gate. |
| 2026-08-16 | Recorded the Python 3.12 CPU test-runner environment issue. |
| 2026-08-16 | Recorded the incorrect real-preflight help-exit expectation. |
| 2026-08-16 | Recorded the Python 3.12 offline Chess build-isolation dependency issue. |
| 2026-08-16 | Reopened CPU work for the v2 startup, sandbox-fork, executor-cleanup, and token-alignment blockers. |
| 2026-08-16 | Closed all reopened CPU review blockers after the 89-test gate. |
| 2026-08-16 | Resolved the training lock's CUDA 13 mismatch with an explicit CPU-only Torch source. |
| 2026-08-16 | Resolved empty-sandbox teardown, sandbox fail-fast/lock handling, and prompt startup side effects. |
| 2026-08-16 | Resolved cache lifecycle propagation and the fork-bank client/logging portion of I-021. |
| 2026-08-16 | Added the CUDA lock and bank-client/side-effect issues from independent code review. |
| 2026-08-16 | Promoted independent-review lifecycle, configuration, and observability findings to CPU blockers. |
| 2026-08-16 | Closed CPU-stage root causes and recorded two remaining GPU-runtime validation risks. |
| 2026-08-16 | Added the reproduced unit-test runner dependency issue. |
| 2026-08-14 | Created issue and blocker register. |

# Issues

| ID | Status | Issue | Root Cause | Planned Resolution |
|----|--------|-------|------------|--------------------|
| I-001 | Open | Repository instructions reference a missing `train/run.sh`. | The file is ignored and not committed. | Use explicit reproducible commands and repository-level `tests/e2e` scripts; document them in `train/integration.md`. |
| I-002 | Resolved | Training dependencies were incomplete. | `train/pyproject.toml` declared only Tinker while source imports local cookbook, tvclient, torch, datasets, and chz. | Declared direct dependencies and editable local packages; generated a Linux x86_64 lock with Tinker 0.24.1. |
| I-003 | Resolved | TVCache port documentation and clients disagree. | README/sync client use 8000; async client and CLI use 8001. | Standardized the implemented rollout path on an explicit configurable port-8001 URL; unrelated legacy docs remain out of scope. |
| I-004 | Resolved | Sandbox video directory was a placeholder. | `sandbox_manager.py` contained `path/to/train/EgoSchema/videos`. | Added required `EGOSCHEMA_VIDEO_DIR` validation and constructor-owned path use. |
| I-005 | Resolved | Cache/network failures could become silent misses or synthetic tool results. | Multiple client and loop boundaries caught infrastructure errors. | Removed the relevant broad conversions and verified exception propagation across all rollout variants. |
| I-006 | Resolved | Root fork-bank environment ID was passed as a task name and the warmed root was not materialized. | Incorrect constructor argument and lazy root publication in the async executor/bank path. | Passed the value as `env_id`, materialized a real forked root, and verified its lifecycle. |
| I-007 | Resolved | Existing server test could report PASS while `/put` returned HTTP 500. | Only a print-oriented test ran; it used the old payload schema and weak assertions. | Added assertion-based integration coverage for the current `values`/`tool_exec_times` schema and HTTP status. |
| I-008 | Resolved | Paper model is unavailable in Tinker. | `Qwen/Qwen3-30B-A3B-Instruct-2507` was retired. | Configured `Qwen/Qwen3.6-35B-A3B` with `qwen3_disable_thinking` in all three training variants. |
| I-009 | Open | Same-prefix request coalescing is not implemented in the active server path. | Documented lock routes are absent and the active immutable tree lacks the claimed lock workflow. | Keep single-process scope, characterize the gap, and defer distributed/coalescing implementation. |
| I-010 | Blocked | Real GPU assets and the Tinker credential are not yet ready for the worker run. | A value-free check in both interactive and non-interactive zsh reports `TINKER_API_KEY=UNSET`; no assignment/reference exists in the checked startup files, so the reported zshrc update is not present in the active home directory. | Write the key to the active user's `~/.zshrc` or enter it through a hidden prompt, then confirm presence only. Complete and verify the DeepSeek/local-vision migration, run `rlaunch --predict-only` before live two-H800 allocation, and prepare external assets on the worker. |
| I-011 | Resolved | The targeted unit tests could not execute in a declared project environment. | The client dev group omitted `pytest-asyncio`, and the client runtime dependencies omitted directly imported `httpx`. | Added both dependencies, regenerated `uv.lock`, and verified 5/5 tests pass in the isolated client environment. |
| I-012 | Resolved | `SandboxManager` startup had an external prompt pull and wrote a cache file in its current working directory. | Constructor initialization called `hub.pull` and persisted `react_prompt_cache.pkl` before the real tool-chain was validated. | Startup now uses the committed module-relative prompt asset and performs no external prompt pull or cwd write. |
| I-013 | Resolved | Rollout-wide exceptions bypassed cleanup. | All three drivers ran unstructured `asyncio.gather`, and the TVCache loop closed internal resources only on the normal path. | Shared structured execution cancels/awaits siblings, attempts every cleanup, preserves primary and cleanup errors, and closes executor/bank/warmup resources. |
| I-014 | Resolved | The drivers could not express the required fixed one-question dataset slice. | Dataset preparation hardcoded the first 100 entries and shuffled before the run; Config had no explicit selector. | All drivers expose validated `dataset_start`/`dataset_count`; the required smoke is `0/1` with `batch_size=1`, `group_size=2`, `epochs=1`. |
| I-015 | Resolved | Rollout logs could fail on first write and collide between baseline and TVCache. | Agent loops appended under an uncreated relative path and variants reused sandbox IDs. | Each run derives and creates its own `config.log_path/rollouts` directory before writing. |
| I-016 | Resolved | Cache lifecycle operations silently degraded. | `get_test_result()` and `unref()` swallowed HTTP errors, while `get_all_envs()` converted service failure into an empty list. | HTTP/status and required-schema failures now propagate; bank deposit cleans unpublished forks and all focused lifecycle tests pass. |
| I-017 | Resolved | Fork-bank root materialization conflicted with the real empty-sandbox lifecycle. | `SandboxManager.stop_sandbox` unconditionally deleted `loaded_video[sandbox_id]`, although an empty started/forked sandbox has no such entry. | Empty sandbox metadata is optional, fork toolkit failure cleans its copied sandbox, and focused lifecycle tests pass. |
| I-018 | Resolved | Sandbox internals converted unexpected failures to plausible values or could deadlock later requests. | Fork and object-query paths caught broad exceptions; missing OpenAI key became a string; VQA manually acquired a lock without guaranteed release. | Failures now propagate, credentials validate before component startup, and VQA uses exception-safe lock ownership. |
| I-019 | Resolved | Required numeric rollout evidence was not connected to training output. | Executor statistics existed but production drivers never collected them, and results omitted cache/fork/token/tool metrics. | All drivers persist JSONL records with stable identity, reward, token, cache/tool/fork/put counts, and elapsed time; proactive warmup forks are included. |
| I-020 | Resolved | The training lock resolved Torch 2.13.0 with CUDA 13 dependencies for an H800 CUDA 12.8 worker. | `torch` was unconstrained even though the remote-Tinker driver uses only CPU tensor construction. | Pinned `torch==2.13.0+cpu` to the explicit official CPU index and regenerated a 139-package lock with zero CUDA/NVIDIA/Triton runtime packages; worker import/tensor preflight remains a Phase 4 validation gate. |
| I-021 | Resolved | Fork-bank and sandbox startup side effects leaked resources or polluted the current directory. | The bank owned an unclosed HTTP client, installed an import-time file logger, and sandbox startup pulled an unpinned prompt into a cwd pickle. | Bank cleanup is owned by the loop; module file logging is removed; SandboxManager loads the committed prompt asset by module-relative path without external startup writes. |
| I-022 | Resolved | All three real training entry points failed before startup. | Each `__main__` block called `asyncio.run(...)`, but none of the three driver modules imported `asyncio`. | Imported `asyncio` explicitly in every driver; the real Python 3.12 preflight imported all 3 modules and confirmed all 3 async `main` functions and native help outputs. |
| I-023 | Resolved | A loaded-only sandbox could not be forked before preprocessing. | The executor forks immediately after `load_video`, while `SandboxManager.fork()` always constructed `ToolKit`, whose constructor required absent preprocessing output. | Rebuild `ToolKit` only when the source sandbox has preprocessing state; focused tests observe 0 toolkit constructions for loaded-only forks and preserve preprocessed-fork cleanup. |
| I-024 | Resolved | Executor resource ownership was detached, silent, and non-transactional. | Detached cleanup and incomplete exception ownership could leak environments, references, clients, or handlers. | Replaced detached cleanup with awaited structured ownership, transactional fork/publication paths, aggregated cleanup errors, and owned handler closure; failure-injection tests pass in the 128-test gate. |
| I-025 | Resolved | Multi-turn training data could associate old logprobs with different rendered tokens. | The non-thinking renderer may produce non-prefix observations, while loops forced every turn into one token array. | All loops now emit official `Transition`/`Trajectory` values and drivers use `trajectory_to_data`; real preflight produced 2 aligned datums from 2 non-prefix observations and 2 sampled tokens. |
| I-026 | Resolved | Core cache lookups and publication accepted malformed HTTP 200 responses as valid results. | `exact_match`, `prefix_match`, and `put` used response defaults instead of requiring the active schema. | Required fields and types at all three boundaries, including successful publication; malformed-schema regressions pass. |
| I-027 | Resolved | TVCache sampling retried every exception up to five times. | Infrastructure, configuration, and unknown errors were treated as retryable. | Removed unconditional retry; the first sampling exception propagates after exactly 1 backend call. |
| I-028 | Resolved | Partial-prefix calls were counted as both prefix hits and misses. | Miss accounting occurred before prefix reuse was known. | Moved accounting to mutually exclusive branches; focused invariants observe exactly one of exact hit, prefix hit, or miss per total call. |
| I-029 | Resolved | Repeated sandbox IDs could silently reuse residual directories. | Sandbox creation allowed existing paths and driver IDs lacked a run namespace. | Sandbox creation now fails on existing directories, and every driver prefixes IDs with variant plus a process run ID; residual-state and ID regressions pass. |
| I-030 | Resolved | Phase 4 service paths and credential handling depended on the process working directory. | Active service paths were relative and `run_sandbox.sh` overwrote `OPENAI_API_KEY` with a placeholder. | Added validated absolute runtime-directory inputs, threaded them through manager/toolkit/services, removed the placeholder export, and verified active-path configuration tests. |
| I-031 | Resolved | Frozen offline sync could not build cached `chess==1.11.2` because its isolated PEP 517 environment could not read `setuptools==84.0.0` from uv's canonical registry cache. | Runtime setuptools was installed, but build isolation owns a separate environment; the lock-verified setuptools wheel existed only in `/data/ycfeng/tmp`. | Installed Chess by exact name with `--offline --find-links /data/ycfeng/tmp --no-deps` while retaining cached registry metadata; Chess built in 1.14 s, plain frozen sync installed the remaining 97 packages, and `uv pip check` reported all 138 installed packages compatible. |
| I-032 | Resolved | The real Tinker preflight rejected valid native driver help output because it expected exit code 0. | chz 0.4.0 intentionally prints `EntrypointHelpException` to stdout and calls `sys.exit(1)`; the vendored official Tinker cookbook has the same behavior. | Corrected only the test contract to require exit 1, empty stderr, and expected fields; the full preflight passed with 2 datums, 2 sampled tokens, and all 3 driver imports/help checks. |
| I-033 | Resolved | The complete Python 3.12 training environment did not include pytest, while a separate frozen client-dev sync could not consume the verified flat Pygments wheel as the lock's canonical registry artifact. | Test dependencies belong to the client `dev` group, not the training lock; frozen sync enforces canonical source identity even with `--find-links`. | Downloaded and verified the four exact client-lock test wheels, installed them temporarily by package name, passed 128/128 tests in 1.62 s, then frozen sync removed exactly 4 packages and restored 138 compatible training distributions. |
| I-034 | Open | Generated untracked test bytecode and a cache-server log remain in the worktree. | Earlier test/integration runs wrote 36 bytecode files and `fork_cache_server.log`; no related process remains live. | Exclude them from review/commit and request explicit user permission before deleting the 36 files (387,469 bytes) and one log (15,802 bytes). |
| I-035 | Resolved | VideoAgent model and prompt assets depended on the caller's current working directory. | The supported launcher executes an absolute Python file without changing directory, while active modules resolved LaViLa, model weights, and the caption prompt through relative paths. | Model directories now come from validated absolute configuration; static prompt and LaViLa resources resolve from their module paths; non-VideoAgent-cwd regressions pass. |
| I-036 | Resolved | A failed fork-bank deposit could retain every cache reference acquired by `get_all_envs()`. | `deposit()` released references only after all forks and root priming succeeded; its exception cleanup stopped created forks but never called `unref()` for returned environment IDs. | Reference release is now part of unified deposit cleanup and preserves primary plus cleanup errors; stored-environment failure regressions pass. |
| I-037 | Resolved | A failed partial-prefix environment switch could lose ownership of the previous rollout sandbox. | The executor assigned the new environment before stopping the old one, so an old-environment stop failure left only the new environment reachable by `close()`. | Ownership transfers only after the old environment stops; failed new-environment cleanup remains owned for `close()`; single- and double-failure regressions pass. |
| I-038 | Resolved | `SandboxManager.fork()` could leave a partial copied directory and advanced fork counter when `copytree()` failed. | Fork metadata advanced before a non-transactional filesystem copy whose exception path had no rollback. | Copy failure now removes partial output and rolls back the counter when directory rollback succeeds; primary and rollback errors remain visible; failure-injection regressions pass. |
| I-039 | Resolved | Sandbox and TVCache defaults bound beyond the task's loopback-only topology. | Sandbox hardcoded `0.0.0.0:5000`; TVCache function/CLI defaults also used `0.0.0.0`, while the design requires `127.0.0.1`. | Sandbox and TVCache function/CLI defaults now bind `127.0.0.1`; entrypoint/default regressions pass. |
| I-040 | Resolved | A later `start_sandbox()` failure could leave TVCache executor handlers or fork-bank clients open. | `run_agent_loops()` cleaned only loops recorded after successful start, while every constructed TVCache loop already owned persistent clients and `start_sandbox()` could open its executor handler. | Central startup failure now cleans every constructed loop; TVCache cleanup withdraws once, independently closes executor/bank exactly once, and preserves all cleanup errors; 4/4 focused and 145/145 CPU pytest cases pass. |
| I-041 | Deferred | Cross-batch warmup forks can outlive the current rollout group in multi-batch training if the driver exits before the next batch consumes them. | Warmup publication targets a future task and no driver-wide owner guarantees withdrawal after an inter-batch failure. The required smoke uses `dataset_count=1`, `batch_size=1`, and `epochs=1`, so `n_train_batches=1`, the `next_batch_idx < n_train_batches` branch is false, and zero warmup deposits are created. | No production change in this task because the path is unreachable in the fixed one-batch reproduction. Define and test driver-wide warmup ownership before any future multi-batch experiment. |
| I-042 | Resolved | A fork whose stop operation fails during `SimpleDictBank.withdraw()` permanently loses bank ownership and cannot be retried by a later withdrawal. | `withdraw()` cleared the task's complete fork map before stopping its entries, recorded stop failures only as exceptions, and never restored the failed fork IDs. | Failed IDs are now merged back under their original task and parent before errors are raised. The focused regression passed 1/1 and the complete bank module passed 8/8; the second withdrawal retries only the failed fork. |
| I-043 | Resolved | A newly created partial-prefix fork could permanently lose executor ownership when its cleanup stop operation failed. | The partial-prefix setup/`unref`/suffix failure paths preserved the stop exception but, unlike the old-environment replacement path, did not register the failed new fork in `_pending_cleanup_environments`. | All three paths now retain failed forks for structured cleanup. The focused regressions passed 3/3 and the complete lifecycle module passed 18/18; after fault recovery, `close()` retries each failed fork. |
| I-044 | Resolved | An unpublished cache child permanently lost executor ownership when cache `put` and child `stop()` both failed. | `_maybe_put_to_cache()` preserved both exceptions but did not add the failed `to_store` child to `_pending_cleanup_environments`, so `close()` could not retry it. | Temporary-environment stop now retains a unique pending owner on failure. The focused regression observes stop count **1→2** after recovery and `close()`; the complete lifecycle module passes 18/18. |
| I-045 | Resolved | A cache-removed environment permanently lost executor ownership when its stop operation failed. | `_handle_removed_envs()` rebuilt a temporary owner and preserved stop errors, but discarded that owner instead of registering it for `close()` retry. | Removed-environment cleanup now uses the same temporary-owner helper. The focused regression observes stop count **1→2** after recovery and `close()`; the complete lifecycle module passes 18/18. |
| I-046 | Resolved | A sandbox successfully published into the cache had no final task/run-level teardown owner and remained active after every rollout executor closed. | Successful publication transferred only the child `env_id` into the prefix tree. The executor intentionally stops only rollout and failed-cleanup environments, so final ownership must begin after all rollouts finish. | Implemented reference-safe atomic task drain, strict server/client transport, and a run-level coordinator. RED was **0/11 PASS**; GREEN was **11/11 PASS**; the Python 3.12 gate was **160/160 PASS**. The v7 architecture review confirmed normal teardown is fixed, and the v7 code/spec/security review returned **APPROVE**; response-loss safety is tracked separately by I-047. |
| I-047 | Resolved | Run teardown could permanently leave a cache-resident sandbox active when a destructive drain, sandbox stop, or ACK committed but its HTTP response was lost. | The protocol retained stable drain/stop IDs and retry ownership, but the production `async with` path called `close()` only once. A transport failure therefore exited `main()` with pending state held only in process memory; a later process could not recover the original drain ID. The earlier unit tests proved manual re-entry rather than production convergence. | Production context exit now retries only `httpx.TransportError` uncertainty on the same lifecycle until pending drain/stop/ACK counts reach **(0,0,0)**; semantic HTTP status errors still execute once and fail fast. Context RED was **1/2 PASS**, GREEN **2/2 PASS**, lifecycle **8/8 PASS**, and the Python 3.12 suite **175/175 PASS**. V9 architecture returned **CLEAR** with **0 BLOCK/0 WATCH**, and code/spec/security returned **APPROVE** with **0/0/0/0** findings. |
| I-048 | Resolved | The proposed DeepSeek credential was disclosed in chat and could not be safely used. | Chat content is retained outside the controlled runtime environment, so the disclosed credential had to be treated as compromised. | The user confirmed the disclosed key was revoked and a replacement was written directly to `~/.zshrc`. Subsequent checks and records may verify presence only and must never read, print, or persist the value. |
| I-049 | Blocked | The active Tinker credential was disclosed in chat and cannot be used for task execution. | Chat content is outside the controlled secret-injection path; the complete key is now considered compromised even though the shell presence check reports `SET`. | Revoke the exposed key in the official Tinker Console, create a replacement, inject it through hidden input or a controlled startup file, and confirm only `SET` before any SDK or worker request. |
| I-050 | Blocked | Tinker training and sampling are blocked after credential authentication. | `tinker==0.24.1` successfully reads `TINKER_API_KEY`, but `ServiceClient().get_server_capabilities()` returns HTTP **402** because the organization has no usable billing balance or enterprise agreement. | Add balance at `https://tinker.thinkingmachines.ai/billing/balance` or attach an enterprise agreement, then rerun the same value-free capability check before any worker request. |
## I-051 StepCode/StepCast Runtime Contract

- Status: Resolved
- Root cause: The repository had no StepCode/StepCast startup configuration, and the provider contract was unverified.
- Resolution: An H200 worker with the specified image and injected credential returned `/v1/models` HTTP **200**, contained `deepseek-v4-flash`, accepted `response_format={"type":"json_object"}`, and accepted a standard `tool_calls`/`tool_call_id` round-trip. It rejected `json_schema` with HTTP **400** (`response_format type is unavailable now`).
- Follow-up: The adapter uses provider-supported `json_object` and validates `Response` locally. The fixed video/tool-service gate remains in Phase 4.

## I-052 Provider Migration Closure

- Status: Resolved
- Root cause: VideoAgent still required `OPENAI_API_KEY` and retained GPT visual fallback paths while the task required local LaViLa/Video-LLaVA and StepCode-backed object-memory reasoning.
- Fix: SandboxManager now reads StepCode provider configuration, captioning uses local LaViLa for on-demand captions with `0/0` API token counts, and ToolKit accepts only local Video-LLaVA VQA.
- Verification: Provider migration and affected lifecycle tests passed **27/27**.

## I-053 H200 StepCode Credential Injection

- Status: Resolved
- Root cause: The initial worker launch did not include the provider credential.
- Resolution: Parsed the local StepCode config's `apiKey` in memory and passed it through `rlaunch --env`; the worker reported only `STEPCODE_API_KEY=SET`. No secret value was recorded.
- Verification: Final H200 protocol smoke completed with exit code **0**.

## I-054 VideoAgent Runtime Assets

- Status: In Progress
- Root cause: The existing VideoAgent sandbox eagerly loads LaViLa, viCLIP, CLIP, DINOv2, and RT-DETR assets during `SandboxManager`/`ToolKit` initialization. The H200 worker's mounted `/data` exposes the downloaded EgoSchema video but no model directory, Video-LLaVA runtime, or matching preprocessing cache.
- Resolution: The user authorized local weights and selected the smallest verified combination. The active registry now selects LaViLa TSF-B/GPT2-base, DINOv2 ViT-S/14, CLIP ViT-B/32, RT-DETR-L, and verified ViCLIP-L. Segment text embeddings use local CLIP instead of OpenAI embeddings. Download and H200 constructor/preprocessing validation remain pending.

## I-055 Video-LLaVA Disk and Scope Gate

- Status: Resolved (2026-09-09); full VQA tool contract retained
- Resolution evidence: filesystem free space rose to **122,784,391,168 B** after user cleanup. The user authorized resuming assets and H800 fallback if H200 is unavailable. CLIP completed at **353,976,522 B** with the official SHA-256; remaining model downloads and GPU runtime verification are in progress under I-054.
- Evidence: The active VQA service hard-codes `LanguageBind/Video-LLaVA-7B` in `video-agent-tools/VideoAgent/video-llava.py:15` and the rollout prompt/schema expose `visual_question_answering`. The smallest verified local VideoAgent bundle is approximately **3.5 GB**; the selective Video-LLaVA checkpoint inventory is approximately **14,933,716,280 B**. The current `/data` filesystem reported **16,403,120,128 B** available. The full Zenodo archive path requires an archive plus extraction workspace of approximately **34.18 GB**.
- Root cause: The current code has no verified smaller VQA checkpoint/backend. 4-bit loading reduces GPU memory only and does not remove the checkpoint bytes needed on disk.
- Impact: Continuing weight downloads cannot guarantee the complete VideoAgent tool contract or leave enough space for preprocessing/runtime assets. A rollout that merely hopes the provider does not call VQA would violate the fail-fast and complete-tool-chain acceptance rules.
- Options: (1) preserve the full tool set and provide at least the required additional persistent disk space before downloading Video-LLaVA; or (2) formally narrow this round's tool schema and acceptance to load/preprocess/segment/caption/object-memory operations, removing VQA from the measured rollout. A new smaller VQA backend would be a separate design and validation task.
- Current action: Stopped the clean CLIP download after **224 MiB** of partial ranges; no formal CLIP output was published. No old mixed `.parts` directory was deleted.

### I-056 — Provider accounting gaps (localized fix verified)

- Observed: run_provider returned result reward 1.0 while loop.get_reward() returned 0; no-cache execution counters remained 0; TVCache returned its initial snapshot; response usage was discarded.
- Root cause: provider transport/loop integration did not export the accounting already available at the response and executor boundaries. TVCache executor counters themselves increment correctly.
- Resolution: preserved the string transport return and retained validated usage_records; synchronized loop final_answer; counted successful no-cache execution; replaced TVCache result.stats with the final combined executor/fork-bank snapshot.
- Verification: controlled direct case passed 2 variants, reward 1, calls/executions/misses 1/1/1 and TVCache fork count 3. Client tests passed 8/8; live provider prompt/completion/total 113/49/162. Real video integration remains pending.

### I-057 — External asset transfer rate and worker direct route (open)

- Observed: model/runtime downloads progress slowly on the CPU master. The original 600 s installers expired before installation; current reduced-concurrency installations remain active. Xet retries did not publish usable blobs; HTTP downloads create explicit resumable bytes. The ranged mirror path is now transferring fixed-revision blobs and will enforce official SHA-256.
- Worker experiment: H200 scheduling succeeded, but direct hf-mirror.com connection timed out at the configured 10 s timeout. RJob tvcache-h200-network-20260909-1706 ended Failed, launcher exit 1. See test_report_2026-09-09_h200_download_route.md.
- Scope: the failed direct worker download route is stopped. CPU downloads continue. H200 GPU capacity remains available; H800 fallback is reserved for capacity failure as authorized.
- Remaining impact: complete local model loading, sandbox/VQA startup, and four rollout acceptance cases await the assets and Python environments.

### 2026-09-10 Video-LLaVA CUDA build incompatibility (OPEN)

- Target: offline four-bit loader on H800/H200 (sm_90).
- Evidence: H800 probe b exited 1 at `bitsandbytes.functional.quantize_4bit -> torch.zeros`: `CUDA error: no kernel image is available for execution on the device`. Torch reports 2.0.1+cu117 and architectures through sm_86. The failure follows successful tokenizer/config resolution and begins at checkpoint shard loading.
- Root cause: default PyPI Torch 2.0.1 resolved the CUDA 11.7 build, which cannot run this reached CUDA operation on Hopper. The StepCast image CUDA runtime does not replace the isolated venv's Torch binaries.
- Scoped remedy under investigation: same Torch 2.0.1 and torchvision 0.15.2 releases with CUDA 11.8 wheels; preserve Transformers 4.31.0, bitsandbytes 0.41.0, model revision and four-bit loading. GPU revalidation is required.
- Route evidence: Basemind public index exposes default wheels but no +cu118 variants for these versions; internal development torch index HTTP 404. Official download.pytorch.org index through the existing proxy timed out during TLS handshake. Direct/current-proxy probes are pending. No CUDA-build replacement has been installed yet.
- Resolved dependency: scipy 1.15.3 installed via Basemind; direct loader import now passes. This resolves the initial missing scipy.stats error, not the CUDA failure.

### 2026-09-10 Resolved: Video-LLaVA Hopper loader environment

- Observed failures: cu117 torch.zeros raised no kernel image; after same-version cu118 installation, bitsandbytes import could not resolve libcusparse.so.11.
- Established causes: actual legacy venv CUDA build lacked Hopper kernel support, and bitsandbytes direct dynamic loading could not discover existing NVIDIA libraries.
- Resolution: official hash-verified Torch/torchvision cu118 wheels plus process-local torch/lib, nvidia/cuda_runtime/lib, nvidia/cusparse/lib.
- Evidence: H200 b full loader PASS, 382.0911647360772 s, 5,408,568,320 GPU allocated/peak bytes, CUDA zeros=0.0, both towers/processors loaded.
- Remaining: real service/tool and rollout validation.

### 2026-09-10 Service launcher interrupted by live shell source edit

- Failure: first real-service attempt exited 127, `t: command not found`, immediately after service readiness; no tool smoke ran.
- Cause: modifying a shell script while Bash still reads it can shift the parser file offset. The agent added an earlier config line during execution.
- Correction: preserve a unique launch snapshot under /data/ycfeng/tmp, pass explicit REPO_DIR, and leave the running snapshot unchanged. The replacement script passes bash -n.
- Cleanup evidence: owned VQA/sandbox/cache process exits 143/143/143; GPU monitor exit 0. Attempt b has been submitted for live revalidation.

### 2026-09-10 Provider mixed JSON/native tool output

- Reproduced: JSON content followed by DSML calls after native tool history, despite response_format=json_object. Direct replay returned HTTP 200 and failed strict schema parsing.
- Correction: explicitly request tool_choice=none; native history remains while next actions are returned only in JSON for local execution. Same-history replay passed schema validation (1.819 s, 2513/231/2744 tokens); 8/8 focused checks passed.
- Evidence: provider-history-original/none JSON under /data/ycfeng/tmp; test report includes exact commands. Full rollout revalidation active in H200 attempt c.

### 2026-09-10 REOPENED: mixed JSON/native provider protocol

`tool_choice=none` did not resolve the real failure (attempt c). Correct JSON arguments and disabled thinking also fail on the actual history. Three pure-text-history JSON probes pass; one declared-native-tools probe passes with structured calls. Both revise the recorded wire protocol. Decision pending; prefer the smaller textual-history adaptation for this bounded acceptance. Full evidence and timings are in progress.md and test_report_2026-09-10_real_video_rollouts.md.


## I-054 / I-057 H800 asset and runtime migration

- Status: Resolved (2026-09-11).
- Resolution: H200 was disabled per user direction. H800 `codesign` was selected using the authoritative worker recipe and company image. The H800-owned cache was assembled with pinned Video-LLaVA/LanguageBind revisions, required tokenizer/config metadata, no duplicate `.bin` shards, and newline-free `refs/main` entries for `huggingface_hub==0.20.3`.
- Verification: offline four-bit loader PASS on H800; Captioning, SegmentFeature, Tracking and tool smoke PASS; four real rollouts PASS.

## I-058 H800 real rollout acceptance

- Status: Resolved (2026-09-11).
- Evidence: `tvcache-real-rollout-h800-20260911-b-rollouts/summary.json` reports `completed_rollouts=4`, `no_cache_rollouts=2`, `tvcache_rollouts=2`, `tvcache_exact_hits=5`, and no remaining run sandboxes. Every rollout has reward `1.0` and final answer index `1`.
- Scope: provider inference plus local JSON action parsing, ordinary-message tool-result history, local tool execution, sandbox lifecycle, and TVCache statistics. No RL optimizer update was run.
- Residual watch: the TVCache log contains a benign warning when withdrawing a task absent from `warmed_environments`; the lifecycle still converged and all required pending drains/stops/ACKs reached zero.
